@model IEnumerable<GUI_assignment_2.Models.CheckedInModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoomNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kids)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Adults)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kids)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adults)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CheckedInModelID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CheckedInModelID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CheckedInModelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
